export enum Routes {
  MAP = "ROUTE_MAP",
  SEARCH = "ROUTE_SEARCH",
  INFO = "ROUTE_INFO",
}

export enum Stacks {
  MAP = "STACK_MAP",
  SEARCH = "STACK_SEARCH",
  INFO = "STACK_INFO",
}

export enum Params {}

export enum Params {}
